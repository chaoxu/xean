# Distribution

The `main` branch at `https://github.com/chaoxu/xean` is the current release. Install and update from that branch. Do not create new numbered releases or release tags. Preserve existing releases and tags as historical archives.

Use exact Git commits and the dependency lockfile for reproducible runs. Package version fields remain compatibility metadata. Workflow and execution-contract schema versions retain their existing meaning and update rules.
