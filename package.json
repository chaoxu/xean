{
  "name": "xean",
  "version": "1.1.1",
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.13"
  },
  "files": [
    "src",
    "patches",
    "vendor",
    "LICENSE",
    "README.md",
    "SPEC.md",
    "docs/application-author.md",
    "examples"
  ],
  "exports": {
    ".": "./src/index.ts",
    "./pi": "./src/pi.ts",
    "./observe": "./src/observe.ts"
  },
  "scripts": {
    "format": "prettier --write . --ignore-path .prettierignore",
    "format:check": "prettier --check . --ignore-path .prettierignore",
    "typecheck": "tsc --noEmit",
    "test": "bun test ./tests ./packages/solve/tests --path-ignore-patterns='runs/**'",
    "test:kernel": "bun test ./tests --path-ignore-patterns='runs/**'",
    "test:solve": "bun test ./packages/solve/tests --path-ignore-patterns='runs/**'",
    "pack:check": "bun scripts/check-package.ts",
    "e2e:roles": "bun test ./packages/solve/tests/role-cli-e2e.test.ts --path-ignore-patterns='runs/**'",
    "lines": "bun scripts/lines.ts",
    "debug": "bun ../xean-lab/src/cli.ts debug --xean-root .",
    "check:fast": "bun run format:check && bun run typecheck && bun run test",
    "check": "bun run check:fast && bun run pack:check && bun packages/solve/solve.ts --help"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.87.0",
    "@earendil-works/pi-ai": "0.87.0",
    "@earendil-works/pi-coding-agent": "0.87.0",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "prettier": "3.9.6",
    "typescript": "7.0.2"
  },
  "bundledDependencies": [
    "@earendil-works/pi-ai"
  ],
  "overrides": {
    "tar": "^7.5.7",
    "@earendil-works/pi-ai": "file:vendor/pi-ai-0.87.0-xean.2.tgz"
  }
}
